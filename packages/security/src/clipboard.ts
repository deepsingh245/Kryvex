/**
 * Phase 1 scaffold only. Real clipboard-clear-after-timeout logic
 * (build spec §20) lands in Phase 7.
 */

export function notYetImplemented(feature: string): never {
  throw new Error(
    `@kryvex/security/clipboard: "${feature}" is not implemented until Phase 7`,
  );
}
