/**
 * Phase 1 scaffold only. Real shared design primitives (build spec §22:
 * modern, minimal, calm — not "hacker" aesthetics) land starting Phase 4/8
 * once web and mobile UI needs are concrete enough to know what's actually
 * shareable across react-dom and react-native. This one token proves the
 * package builds, lints, typechecks, and is importable from `apps/web`.
 */

export const KRYVEX_BRAND_COLOR = "#1f2937" as const;
